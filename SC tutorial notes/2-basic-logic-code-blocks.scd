(
"First Line".postln;
"Second Line".postln;
(2+2).postln;
3 + 3;
"Finished".postln;

// we can execute multiple lines of code using code blocks.
// we simply enxloce in parentheses all lines of code that we want to run together, and we place the cursor anywhere inside the parenthesis and we press CTRL + ENTER to execute the code block. All of the lines will be evaluated almost  simultaneously (from top to bottom).
)